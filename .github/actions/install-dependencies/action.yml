name: "Install dependencies"
runs:
  using: "composite"
  steps:
    - uses: actions/setup-python@v3
      with:
        python-version: '3.11'
    - shell: bash
      run: |
        python -m pip install --upgrade pip poetry
        poetry install
